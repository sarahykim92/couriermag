.issue
  - page_articles.each_with_index do |article, i|
    .issue-article
      - if article.metadata[:page]['hero']
        %img.hero{src: article.metadata[:page]['hero']}
      %h2
        = link_to article.title, article
        %span= article.date.strftime('%b %e')

.river
  = partial "river"