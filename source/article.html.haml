- if current_article
  = wrap_layout :layout do
    .article
      - if current_article.metadata[:page]['hero']
        %img.hero{src: current_article.metadata[:page]['hero']}

      %h1= current_article.title
      = yield

    .river
      = partial "river"